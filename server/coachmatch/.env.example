# Modelo de variáveis de ambiente — copie para `.env` (não versionado).
#
# Perfil AWS usado nos deploys manuais (login via SSO: `aws sso login --profile <perfil>`).
# O Serverless v4 carrega este .env automaticamente.
AWS_PROFILE=JoaoAdm
