{
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "scripts": {
    "dev": "serverless offline start --stage local",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:fn": "serverless deploy function --stage dev --function",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1037.0",
    "@aws-sdk/client-sqs": "^3.1069.0",
    "@aws-sdk/lib-dynamodb": "^3.1051.0",
    "aws-jwt-verify": "^5.1.1",
    "dotenv": "^17.4.2",
    "joi": "^18.1.2",
    "serverless-http": "^4.0.0",
    "stream-chat": "^9.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "esbuild": "^0.28.1",
    "serverless": "^4.36.1",
    "serverless-dynamodb": "^0.2.58",
    "serverless-esbuild": "^1.57.2",
    "serverless-offline": "^14.5.0",
    "vitest": "^3.2.4"
  }
}
